### ORGANIZATION

The versions of the protocol with preprocessing are available here.
Each Version has their own README.md with requirements and build instructions.

The approximate version of the protocol uses a C++ implementation of Apple's Detectable Hash Function (DHF).
The setup requires pybind11  to call the C++ library.
More details are available in the appox/README.md
